// The founding admin account. Kept in sync with functions/lib/config.js —
// its admin access can never be revoked, so the UI hides that action too.
export const PRIMARY_ADMIN_EMAIL = 'achi9944@gmail.com'
